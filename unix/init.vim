# setup for nvim
# sudo apt install neovim
# mkdir ~/.config/nvim
# touch ~/.config/nvim/init.vim

# use vim settings for nvim
set runtimepath+=~/.vim,~/.vim/after
set packpath+=~/.vim
source ~/.vimrc
